
  <style>
    :root {
      --mcd-red: #db0007;
      --mcd-yellow: #ffbc0d;
      --mcd-dark: #27251f;
    }
    body {
      background-color: #f5f5f5;
    }
    .card-food:hover {
      transform: translateY(-5px);
      transition: transform 0.3s;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    }
    .btn-yellow {
      background-color: var(--mcd-yellow);
      color: var(--mcd-dark);
      font-weight: bold;
    }
    .btn-yellow:hover {
      background-color: #e6a800;
      color: var(--mcd-dark);
    }
    .card-img-top {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }
  </style>
    <header class="text-center py-5">
      <h1>Welcome to !</h1>
      <p>Lorem, ipsum dolor sit amet consectetur adipisicing elit. Assumenda ea illum, blanditiis sed voluptatem pariatur, quod exercitationem error quae fuga amet accusantium placeat delectus sequi corporis fugiat ducimus excepturi dolor.</p>
    </header>
    <main class="container">
      <section class="text-center my-5">
        <a href="/catalog" class="btn btn-yellow btn-lg">Start Ordering</a>
      </section>
      <section class="text-center my-5">
        <h2>Our reviews</h2>
        <div class="row">
          <% recensies.forEach(recensie => { %>
            <div class="col-md-4 col-lg-4 mb-4 col-12 menu-item">
              <div class="card card-food h-100">
                <div class="badge bg-warning position-absolute m-2 top-0 end-0"></div>
                <img src="<%= recensie.image %>" class="card-img-top" alt="<%= recensie.title %>" />
                <div class="card-body d-flex flex-column">
                  <h5 class="card-title"><%= recensie.title %></h5>
                  <p class="card-text flex-grow-1"><%= recensie.text %></p>
                  <div class="d-flex justify-content-between align-items-center mt-2">
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      </section>
    </main>
    <footer class="text-center py-4">
      <p>&copy; 2025 BurgerNow! All rights reserved.</p>
    </footer>
    <script src=""></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="js/scripts.js"></script>


